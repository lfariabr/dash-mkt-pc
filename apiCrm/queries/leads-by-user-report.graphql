query ($filters: LeadsByUserReportFilters!) {
    leadsByUserReport(filters: $filters) {
        data {
            name
            shiftNumber
            messagesCount
            uniqueMessagesCount
            messagesCountByStatus {
                code
                label
            }
            successRate
        }
    }
}
