query ($filters: FollowUpsCommentsReportFilters!) {
    followUpsCommentsReport(filters: $filters) {
        data {
            commentsCount
            commentsCustomerIds
            name
        }
    }
}
